import React from 'react';
import UserGridComponent from '../../components/UserGrid/UserGridComponent';

function UserListView(){
    return(
        <div>
            <UserGridComponent/>
        </div>
    );
};

export default UserListView;